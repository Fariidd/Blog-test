{{ form_start(form) }}
    {{ form_row(form.content,{'attr':{'class':'form-control'},'label':'Commentaire'}) }}
    <button class="mt-4 btn btn-primary">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
